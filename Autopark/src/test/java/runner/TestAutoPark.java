package runner;

import Autopark.AutoPark;
import Autopark.Vehicle;
import org.testng.annotations.BeforeTest;
import org.testng.annotations.DataProvider;
import org.testng.annotations.Test;

import java.util.ArrayList;
import java.util.List;

/**
 * Created by Dmitry on 11.12.2016.
 */
public class TestAutoPark {
  /*  protected Vehicle vehicle;
    protected AutoPark autoPark;
    protected List<Vehicle> cars;

    @BeforeTest()
    public void setUp() {
        autoPark = new AutoPark();
        vehicle  = new Vehicle(1.0, 750, "Lada", 1989);
        vehicle  = new Vehicle(1.3, 2750, "Jeep", 1999);
        cars = new ArrayList<>();
    }
   /* @DataProvider(name = "data")
    public Object[][] data(){
        return  new Object[][]{
                {1.0, "lada", 1989, 750},
                {1.0, "lada", 1989, 750}
        };
    }*/

}
